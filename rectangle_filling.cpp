#include <bits/stdc++.h>
using namespace std;

void solve() {
		int n, m; cin >> n >> m;
		string rect[n];
		for (int j=0; j<=n; j++) {
			cin >> rect[j];
			//cout << rect[j] << endl;
		}
		

	}

int main() {
	int tc; cin >> tc;
	for(int i = 1; i <= tc; i++) {
		//cout << "TEST CASE# " << i << endl;
		solve();
	}
	
}